syntax = "proto3";

message StreamSnippet {
  string          body = 1;
  string          userId = 2;
  int64           createdOnUtc = 3;
  repeated string tags = 4;
  repeated string mentions = 5;
}
